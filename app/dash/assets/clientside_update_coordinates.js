window.dash_clientside = Object.assign({}, window.dash_clientside, {
    clientside_update_coordinates: {
        update_coordinates: function (
            n_clicks,
            map_type
        ) {
            console.log(window)
            // if (map_type === 'sem_tipo' || n_clicks % 2 === 1) {
                // console.log(window.areaAluguel)
                // console.log(window.valorAluguel)
                // console.log(window.coords)
                // console.log(window.myLeafletMap._popup)
                // console.log(window.myLeafletMap._popup._latlng);
                // return [0, 0 , 0 , 0];
            // }
        }
    }
});
